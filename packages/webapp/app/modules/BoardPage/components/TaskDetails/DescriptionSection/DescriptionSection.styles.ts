import { css } from '@tma/design-system';

export const descriptionInputAndButton = css({
  display: 'flex',
  flexDirection: 'column',
  rowGap: 'sm',
});
export const descriptionButtons = css({
  display: 'flex',
  columnGap: 'xs',
});
