import { styled } from '@tma/design-system';
import * as ModuleLayout from '../../layouts/ModuleLayout';

export const DepartmentContent = styled(ModuleLayout.Content, {
  base: {
    flexWrap: 'wrap',
    alignContent: 'flex-start',
  },
});
