import { SetValueConfig } from 'react-hook-form';

export const defaultSetValueConfig: SetValueConfig = {
  shouldDirty: true,
  shouldValidate: true,
};
export const DEFAULT_ERROR_MESSAGE = 'Unknown error';
