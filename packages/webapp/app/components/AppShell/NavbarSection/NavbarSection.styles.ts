import { css, flex } from '@tma/design-system';

export const titleRoot = css({
  px: 'md',
});
export const titleContent = flex({
  justifyContent: 'space-between',
  alignItems: 'center',
});
export const root = flex({
  rowGap: 6,
  flexDirection: 'column',
});

export const content = css({
  px: 6,
});
